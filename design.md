# Aman Design System

> UI 设计风格原则 & 组件体系规范
>
> 版本: v1.0 | 最后更新: 2026-06-01

---

## 目录

1. [设计哲学](#1-设计哲学)
2. [视觉语言](#2-视觉语言)
3. [布局与空间](#3-布局与空间)
4. [组件原则](#4-组件原则)
5. [动效与动画](#5-动效与动画)
6. [状态与边界](#6-状态与边界)
7. [文案与语气](#7-文案与语气)
8. [可访问性](#8-可访问性)
9. [页面设计模式](#9-页面设计模式)
10. [设计令牌速查](#10-设计令牌速查)

---

## 1. 设计哲学

### 1.1 核心精神：不卑不亢

Aman 的 SOUL 写着一句话——"不卑不亢。不需要讨好用户，也不需要证明自己聪明。"

UI 设计继承同样的精神：

- **不讨好** — 不用糖果色、表情符号雨、夸张的庆祝动画来"取悦"用户。好工具不需要卖萌。
- **不炫耀** — 不堆砌无意义的数据可视化、不使用过度复杂的动画来显得"高端"。简洁来自于自信，自信来自于能力。
- **不沉默** — 出错时坦然告知，成功时简洁确认。不隐藏信息，不粉饰问题。

### 1.2 设计公理

**"万物皆事件，响应即行为。"**

这条后端架构公理对 UI 有三层映射：

| 后端公理 | UI 映射 |
|---------|--------|
| 一切外部输入都是事件 | UI 中每个状态变化都应有可追溯的来源。界面不"凭空"改变。 |
| 一切内部行为都是响应 | 用户操作后，UI 必须在 200ms 内给出可感知的反馈。没有操作的界面应该是静止的。 |
| 没有"主循环等待" | 界面不应有"等待用户做某事"的僵死状态——即使空闲，也要有呼吸感。 |

### 1.3 设计价值观

**实用优先于美观。**
每个像素都应有目的。装饰性元素（分隔线、阴影、渐变）只有在传递信息层级时才使用。如果去掉一个元素不影响理解，那就去掉它。

**信息密度优先于留白。**
Aman 是工具，不是营销页面。参考 Bloomberg 终端和 Linear 的信息密度——让用户在一屏内看到更多有用信息，而不是用留白"呼吸"掉半屏空间。

**可见状态优先于隐藏状态。**
Aman 的后端有 7 级背压、12 种事件类型、8 种空闲状态。UI 不应隐藏这些——它们不是"实现细节"，它们就是用户需要理解的东西。每个状态都应在某处可见（但不一定始终可见）。

**本地优先，离线可用。**
Aman 是本地应用，不是云服务。UI 不应给用户"我在等待服务器"的感觉——所有操作应该立刻响应，后台同步应静默进行。

**双语等权。**
中英文内容混合是 Aman 的 DNA。UI 框架必须同时支持两种语言的排版——CJK 和 Latin 的字体、行高、断行规则不同，必须分别处理。Sidebar 标签、按钮文案、提示文本的默认语言应跟随系统语言设置，但可以在任意位置混排。

---

## 2. 视觉语言

### 2.1 色彩系统

Aman 是**深色优先**的应用。色彩系统的设计目标是：在长时间使用中不造成视觉疲劳，同时保持足够的信息对比度以支撑高密度信息展示。

#### 表面层级（从深到浅）

```
用途            暗色主题                  亮色主题
────────────── ────────────────────────  ────────────────────────
根背景          #0b0d13 (深海军蓝/石板)    #f6f6f8
抬高表面         #11141c                    #ffffff
卡片背景         #161923                    #ffffff
悬停态          #1e2130                    #f0f0f3
激活态          #262a3a                    #e8e8ed
```

**原则：** 表面层级不超过 5 级。每一级之间对比度约 4-6%，刚好可区分但不割裂。

#### 文本层级（从亮到暗）

```
用途            暗色主题                  亮色主题
────────────── ────────────────────────  ────────────────────────
主要文本         #e4e6f0 (浅灰白)          #1c1c24
次要文本         #9a9db5 (灰蓝)            #5c5c6e
辅助文本         #6b6e80 (暗灰)            #8e8e9c
```

**原则：**
- 主要文本在 UI 中占比约 60%，次要约 25%，辅助约 15%
- 辅助文本 (`fg-dim`) 从不用于正文段落——仅用于标签、占位符、时间戳
- 暗色主题下正文不用纯白 (`#fff`)，避免眩光。`#e4e6f0` 的对比度约 13:1，超过 AAA 标准

#### 强调色

```
用途            暗色主题                  亮色主题
────────────── ────────────────────────  ────────────────────────
主强调          #5b7df5 (蓝)              #4a6cf7
强调悬停         #7b98ff                    #3b5de7
强调弱化         rgba(91,125,245,0.12)     rgba(74,108,247,0.08)
```

**强调色使用规则：**
- 每个屏幕最多一个强强调元素（主按钮、选中态链接、当前步骤指示器）
- `accent-muted` 用于选中背景、标签背景、进度条底色
- 强调色**绝不用**于纯装饰（分隔线、阴影、图标底色）
- 图表/数据可视化中的强调色不受"一个强强调元素"限制——数据有独立配色方案

#### 语义色

```
用途            暗色主题                  亮色主题
────────────── ────────────────────────  ────────────────────────
成功            #3dbf6e                    #2d9d5a
警告            #e5b80b                    #b8940a
错误            #e55555                    #d14343
信息            (使用主强调色)              (使用主强调色)
```

**规则：**
- 语义色仅用于状态指示器、徽章、表单验证消息。不用于按钮（除危险操作外）
- 红色 (`red`) 保留给需要用户立即注意的事项：失败、危险操作、关键告警
- 黄色 (`yellow`) 用于需要注意但不紧急的事项：警告、待处理、过期提醒
- 绿色 (`green`) 用于确认完成、正常运行、连接活跃
- 每个语义色都有对应的 `-muted` 变体（约 12% 不透明度），用于大面积背景着色（徽章背景、表格行背景）

#### 边框

```
用途            暗色主题                  亮色主题
────────────── ────────────────────────  ────────────────────────
默认边框         #1e2130                    #e4e4ea
强调边框         #2e3142                    #d0d0d8
```

**规则：** 边框只用于分隔交互区域（卡片、输入框、表格）。不用于纯装饰性分隔——用留白代替。

#### 色彩使用红线

- ❌ 不使用渐变作为主要设计元素（按钮、标题、背景）
- ❌ 不使用超过 6 种不同颜色在同一屏幕上
- ❌ 语义色仅用于传达状态，不用于品牌/装饰
- ✅ 数据可视化可以用独立的分类调色板（最多 8 色）
- ✅ Agent 头像/图标可以使用独立配色，但需限制在 3-4 个预设颜色内

#### 配色方案速查

以下配色方案直接可用于设计和开发，每个方案都标注了对应的 CSS 变量。

**卡片配色（工具型页面：Dashboard、Home、Workflow）**

```
卡片元素       暗色主题变量              视觉效果
───────────── ──────────────────────── ──────────────────
卡片背景       --bg-card                深色卡片浮于深色背景上
卡片边框       --border                 微弱分隔，不割裂
卡片标题       --fg (14px/600w)         高对比，可扫读
卡片正文       --fg-secondary (13px)    次要信息，不抢眼
统计数值       --accent (28px/700w)     最醒目的元素
统计标签       --fg-dim (11px/500w)     安静标注
卡片悬停       border→--accent          交互反馈
              translateY(-2px)
```

**对话配色（Chat 页面）**

```
气泡元素       暗色主题变量              视觉效果
───────────── ──────────────────────── ──────────────────
用户气泡       bg: --accent             蓝色实心，靠右
              text: #fff
AI 气泡        bg: --bg-card            深色卡片，靠左
              border: --border
系统消息       --fg-dim (12px)          居中，淡色，不打扰
流式输出       border: --accent         蓝色边框+微光环
              shadow: accent-muted
中断消息       border: dashed --yellow  黄色虚线边框
归档消息       opacity: 0.5            半透明
```

**状态指示配色（Sidebar、Agent Card、Dashboard）**

```
状态          配色                             使用场景
───────────── ──────────────────────────────── ──────────────────
运行中/在线    --green 背景 + --green 文字       Gateway 状态点、Agent 活跃
              dot: 0 0 6px green glow
已停止/离线    --red 背景 + --red 文字           Gateway 离线、任务失败
过渡中/等待    --yellow 背景 + --yellow 文字     启动中、队列中
未知/初始      --fg-dim 背景 + --fg-dim 文字    未初始化状态
```

**Agent 空闲环配色（IdleRing 组件）**

```
Agent 状态    外环色        内环色        含义
───────────── ────────────  ────────────  ──────────────────
idle          #6c8cff       #f59e0b       空闲，等待事件
reflection    #a78bfa       #f472b6       反思中，消化信息
chatting      #4ade80       #22d3ee       活跃对话
working       #f59e0b       #fbbf24       执行任务
studying      #a78bfa       #c4b5fd       学习中
daily_life    #fb923c       #fdba74       日常例程
waiting       #f59e0b       #fbbf24       等待外部结果
```

> 这些颜色是 Agent 个性的一部分，不映射到 CSS 语义变量。通过组件 props 传入，不作为全局令牌。

**数据可视化分类调色板（事件日志、图表）**

```
分类          颜色        用途
───────────── ──────────  ──────────────────
默认/通用      #93c5fd    普通事件
LLM 调用       #a78bfa    LLM 相关事件
工具执行       #34d399    Tool 调用事件
消息收发       #fbbf24    消息类事件
能力/技能      #60a5fa    能力相关事件
警告事件       #fb923c    需要注意的事件
错误事件       --red      错误事件（使用语义色）
```

> 8 色上限。`error` 使用 `--red` 语义色保持一致性，其余 6 色为独立调色板。

**通知横幅配色**

```
严重级别      背景                              文字     图标
───────────── ──────────────────────────────── ──────── ────
Critical      color-mix(srgb, --red, 96%)       white    ⚠
Warning       color-mix(srgb, --yellow, 95%)    #1a1a1a  ⚡
Info          color-mix(srgb, --green, 94%)     white    ℹ
```

**Agent 头像配色（预设 4 色）**

```
索引  背景色      适用场景
───── ──────────  ──────────
0     #5b7df5     通用 Agent (aman)
1     #3dbf6e     健康/生活类 Agent
2     #f59e0b     金融/投资类 Agent
3     #a78bfa     学习/研究类 Agent
```

> Agent 头像颜色由 `agent.key` 的 hash 值决定（`hash % 4`），保证同一 Agent 颜色始终一致。文字始终为 `#fff`。

**表面层级使用场景**

```
变量            使用场景                                  不用于
────────────── ──────────────────────────────────────── ──────────────
--bg           页面主背景、输入框背景                    卡片
--bg-elevated  侧边栏、下拉面板、调试面板背景            主内容区
--bg-card      卡片、消息气泡(AI)、模态框内容            全局背景
--bg-hover     悬停态、骨架屏、选中行、标签背景          —
--bg-active    激活态、按下态、进度条底色                 —
```

**深/亮双主题切换要点**

| 关注点 | 暗色主题策略 | 亮色主题策略 |
|--------|------------|------------|
| 表面区分 | 越上层越亮（`#0b0d13` → `#262a3a`） | 越上层越暗（`#f6f6f8` → `#e8e8ed`） |
| 阴影感知 | 需要更重的阴影（黑 0.3-0.6 不透明度） | 更轻的阴影（黑 0.05-0.12 不透明度） |
| 强调色亮度 | 略亮（`#5b7df5`） | 略暗（`#4a6cf7`） |
| 文本对比度 | 暗底亮字，不超 14:1 | 亮底暗字，不超 14:1 |
| 语义色饱和度 | 略高 | 略低 |

### 2.2 字体系统

#### 字体栈

```css
/* UI 字体 — 系统原生，零加载 */
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
             Roboto, Helvetica, Arial, sans-serif;

/* 等宽字体 — 代码、数据、时间戳 */
font-family: "SF Mono", "Fira Code", "Cascadia Code",
             "JetBrains Mono", "Consolas", monospace;
```

**原则：**
- 不引入 Web 字体。Aman 是桌面应用，使用系统原生字体可获得零延迟渲染和一致的系统体验
- CJK 字体由系统自动匹配（macOS 使用苹方，Windows 使用微软雅黑，Linux 使用 Noto Sans CJK）
- 等宽字体用于：代码块、JSON 数据、事件日志、配置文件、数值表格

#### 字体大小刻度

```
用途            大小      字重      行高
────────────── ────────  ────────  ────────
页面标题         20px      700       1.3
区块标题         16px      600       1.4
卡片标题         14px      600       1.4
正文            14px      400       1.5
辅助文案         13px      400-450   1.5
标签/徽章        11px      500-600   1.3
微小文本         10px      500-600   1.3
代码            13px      400       1.6
```

**规则：**
- 正文基准为 14px（不是常见的 16px）——这是工具型应用的惯例，允许更高信息密度
- 字体大小不得超过 6 级
- 最小字体 10px（绝不低于此值，保证可读性）
- CJK 文本在相同字号下视觉偏大，标签和辅助文案可考虑降低 0.5-1px（通过 `:lang(zh)` 选择器）

#### 字重使用

```
字重      使用场景
───────  ──────────────────────────
400      正文、输入框内容、表格数据
450      导航链接、次要按钮文字
500      标签、徽章、按钮文字
600      卡片标题、区块标题、表头
700      页面标题、数值展示（.stat .value）
```

**原则：**
- 同一屏幕上不超过 4 种字重
- 700 字重仅用于需要用户首先注意的元素（通常每屏 1-2 处）
- 正文绝对不用粗体——用颜色或间距区分层级，而非字重

### 2.3 圆角

```
用途            大小      应用场景
────────────── ────────  ──────────────────────
sm              4px      徽章、标签、键盘快捷键、代码块内联
md              6px      按钮、输入框、下拉菜单、侧边栏导航项
lg              8px      卡片、面板、表格容器
xl              12px     模态框、对话框、大型浮层
```

**规则：**
- 圆角与元素尺寸成正比——大元素用大圆角，小元素用小圆角
- 嵌套元素中，内层圆角 ≤ 外层圆角
- 聊天气泡是例外：使用 18px 圆角（较大，营造对话感）
- 不使用全圆角（pill shape，999px），除非是状态点（6px 圆 = 正圆）

### 2.4 阴影

```
用途            暗色主题                                亮色主题
────────────── ──────────────────────────────────────  ────────────────────────────
sm              0 1px 2px rgba(0,0,0,0.3)              0 1px 2px rgba(0,0,0,0.05)
md              0 4px 12px rgba(0,0,0,0.4)             0 4px 12px rgba(0,0,0,0.07)
lg              0 8px 24px rgba(0,0,0,0.5)             0 8px 24px rgba(0,0,0,0.09)
xl              0 12px 40px rgba(0,0,0,0.6)            0 12px 40px rgba(0,0,0,0.12)
```

**规则：**
- 阴影仅用于浮层（弹出菜单、模态框、通知横幅、工具提示）
- 卡片不使用阴影——边框 (`border: 1px solid var(--border)`) 足以区分层级
- 暗色主题下的阴影比亮色主题更重——在深色背景上需要更大的对比度差异才能感知层级
- 不使用彩色阴影，不使用辉光效果（除状态指示灯外）

### 2.5 图标

**原则：**
- 优先使用简单的几何图标（线条风格，14-18px），不使用填充风格
- 图标传达功能，不是品牌——不需要定制图标集
- 语义色的图标保留给状态指示器（成功/警告/错误点、连接状态）
- 操作图标（编辑、删除、添加、搜索）使用次要文本色
- 文件类型图标使用预设的 4-5 种颜色区分（代码蓝、文档黄、图片绿、数据紫）

---

## 3. 布局与空间

### 3.1 应用骨架

```
┌──────────────────────────────────────────────────────────┐
│ ┌──────────┐ ┌──────────────────────────────────────────┐│
│ │          │ │                                          ││
│ │ Sidebar  │ │           Main Content Area              ││
│ │ 220px    │ │           flex: 1                        ││
│ │          │ │           padding: 24px 28px             ││
│ │          │ │                                          ││
│ │ 可切换为  │ │                                          ││
│ │ 52px     │ │                                          ││
│ │ 紧凑模式  │ │                                          ││
│ │          │ │                                          ││
│ └──────────┘ └──────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────┘
```

**侧边栏规则：**
- 默认宽度 220px，紧凑模式 52px（仅图标）
- 始终存在，不可完全隐藏
- 使用 `border-right: 1px solid var(--border)` 分隔
- 紧凑模式下显示迷你状态点（运行状态、通知计数）
- 切换动画 200ms ease

**主内容区规则：**
- padding: 24px 28px（上下 24px，左右 28px）
- 垂直可滚动，水平不可滚动
- 最大内容宽度不限制（工具型应用不需要"阅读最佳宽度"）

### 3.2 间距刻度

```
缩写    大小      使用场景
────── ────────  ──────────────────────────
xs      4px      图标与标签间距、徽章内边距
sm      8px      紧密相关的元素间距
md      12px     组件内元素间距（默认）
lg      16px     卡片内间距、网格间距
xl      20px     卡片 padding、区块间距
2xl     24px     页面 padding top/bottom
3xl     28px     页面 padding left/right
```

**原则：**
- 间距体系为 4px 基准的线性刻度（4, 8, 12, 16, 20, 24, 28）
- 不使用奇数间距值（对齐到 4px 网格）
- 垂直间距遵循"就近原则"——相关元素更近，不相关元素更远

### 3.3 网格系统

```css
.grid-2 { grid-template-columns: 1fr 1fr; }
.grid-3 { grid-template-columns: 1fr 1fr 1fr; }
.grid-4 { grid-template-columns: 1fr 1fr 1fr 1fr; }
```

**规则：**
- 使用 CSS Grid 而非 Flexbox 进行页面级布局
- 默认网格间距: 16px (lg)
- 固定宽度侧边栏 + 弹性主区域，不是全部弹性
- 卡片在网格中高度一致（`align-items: stretch`），不出现"参差不齐"的卡片排列

### 3.4 响应式

Aman 当前的目标形态是桌面应用（1200×800 默认窗口，900×600 最小）。响应式断点：

```
断点        最小宽度    布局调整
────────── ──────────  ──────────────────────────
Desktop    1100px+     侧边栏 220px + 双列/三列网格
Compact    900-1099px  侧边栏 52px（强制紧凑）+ 单列或双列
Minimum    900px       绝对最小窗口宽度，不可更小
```

**规则：**
- Aman 是桌面应用，不针对移动端设计布局。900px 以下的行为是"不做布局适配，允许水平滚动"
- 侧边栏在 < 1100px 时自动切换到紧凑模式
- 卡片网格在 < 1100px 时从三列变为双列，在 < 960px 时变为单列

---

## 4. 组件原则

### 4.1 按钮

按钮有三层变体，按"操作重要性"递减排列：

```
变体        样式                        用途
────────── ──────────────────────────  ──────────────────────
Primary    accent 背景 + 白色文字       主要操作（每屏最多一个）
Secondary  bg-hover 背景 + 边框         次要操作、取消、返回
Danger     red 背景 + 白色文字          不可逆破坏性操作
Ghost      无背景无边框 + fg-dim 文字   最低优先级（折叠、展开、更多）
```

**交互规则：**
- hover: 背景色变亮/变暗 10-15%
- active: `transform: scale(0.98)`（按压反馈）
- disabled: `opacity: 0.4` + `cursor: default` + 无 hover 效果
- focus-visible: `outline: 2px solid var(--accent)` + `outline-offset: 2px`
- 按钮文字从不折行；最小宽度 64px（容纳两个汉字）

**尺寸：**
```
尺寸      高度      padding         字号
───────  ───────  ──────────────  ──────
Default  36px     8px 20px        13px
Small    28px     6px 12px        12px
Large    44px     12px 24px       14px
```

### 4.2 输入框

```
状态        样式
────────── ──────────────────────────
默认        bg 背景 + border 边框
悬停        border 颜色 → border-strong
聚焦        border 颜色 → accent (无外发光, 无 outline ring)
禁用        opacity: 0.4
错误        border 颜色 → red
```

**规则：**
- 输入框不使用外发光 (box-shadow glow)——边框颜色变化足以传达状态
- 等宽字体作为默认输入字体
- placeholder 颜色: `fg-dim` + `opacity: 0.7`
- 文本域垂直可调整大小，最小高度 80px
- 单行输入框高度 36px

### 4.3 卡片

```css
.card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 20px;
}
```

**规则：**
- 卡片不使用阴影——边框区分层级已经足够
- 卡片可以有 hover 效果：`translateY(-2px)` + `box-shadow: var(--shadow-md)`（仅在可点击的卡片上）
- 卡片标题：14px / 600 字重 / 底部 12px 间距
- 卡片内信息密度应高——空卡片是设计失败的标志
- 不使用卡片嵌套卡片（保持层级扁平）

### 4.4 状态徽章

```css
.badge {
  display: inline-block;
  padding: 2px 8px;
  border-radius: var(--radius-sm);  /* 4px */
  font-size: 11px;
  font-weight: 500;
}
```

**变体：**
```
类型      背景              文字颜色
──────── ────────────────  ─────────
ok       green-muted       green
warn     yellow-muted      yellow
error    red-muted         red
info     accent-muted      accent
neutral  bg-hover          fg-dim
```

**规则：**
- 徽章只传达状态，不可交互
- 一行最多 3 个徽章；超过则折叠为 "+N"
- 徽章文字不超过 2 个词（中文 4 个字）

### 4.5 状态指示点

```
颜色      含义                        使用场景
──────── ──────────────────────────  ────────────────────
green    运行中 / 活跃 / 已连接       Gateway 状态、Agent 在线
yellow   启动中 / 过渡 / 等待         启动阶段、任务队列中
red      已停止 / 错误 / 断连         Gateway 离线、任务失败
dim      未知 / 未初始化              初始加载状态
```

**规则：**
- 指示点为 6px 直径正圆
- 活跃状态加微弱辉光：`box-shadow: 0 0 6px var(--green) at 50% opacity`（通过 `color-mix(in srgb, var(--green) 50%, transparent)`）
- 指示点可以有动画：活跃时呼吸脉冲（2s 周期，opacity 0.6 → 1.0 → 0.6）

### 4.6 表格

**规则：**
- 表头: 11px / 600 字重 / 全大写 / 0.04em 字间距 / `fg-dim` 颜色
- 数据行: 13px / 400 字重 / `fg` 颜色
- 行分隔线: `border-bottom: 1px solid var(--border)`
- 行高: 36px（单行数据），可扩展到 48px（双行数据）
- 悬停行: `bg-hover` 背景
- 数字列右对齐，文本列左对齐
- 空状态: 显示 `—` (em dash) 而非留空
- 不使用斑马纹（交替行背景色）——对于少于 20 行的表格是多余的信息

### 4.7 对话框 / 模态框

**规则：**
- 最大宽度 520px（避免过宽的对话框）
- 最小宽度 360px
- 背景遮罩: `rgba(0, 0, 0, 0.6)` + `backdrop-filter: blur(2px)`
- 入场动画: `scale(0.95 → 1.0)` + `opacity(0 → 1)`，持续 150ms
- 按 Escape 关闭，点击遮罩关闭（破坏性操作对话框除外）
- 标题 + 正文 + 操作按钮（右对齐，Primary 在右）

### 4.8 Toast / 通知横幅

```
位置        动画              持续时间
────────── ────────────────  ──────────
右下角      slideIn 200ms    自动消失 4s
顶部居中    滑入 + 滑出       手动关闭或自动消失
```

**变体：**
- 通知浮层（toast）：右下角，操作反馈，自动消失
- 通知横幅（overlay）：顶部居中，关键告警，需手动关闭
- 通知下拉（bell）：右上角下拉菜单，通知历史列表

**规则：**
- Toast 最多同时显示 1 个——新 toast 替换旧 toast（不堆叠）
- 横幅最多 1 个（更高级别的通知）
- 通知文案不超过两行

---

## 5. 动效与动画

### 5.1 核心原则

> "动画是信息，不是装饰。"

Aman 的动画哲学：

1. **快速** — 所有交互动画 ≤ 200ms。不是"让用户欣赏"，而是"让用户感知"。
2. **目的明确** — 每个动画对应一种信息：层级变化（缩放/淡入）、位置变化（滑动）、状态变化（颜色过渡）。
3. **可跳过** — 所有动画遵守 `prefers-reduced-motion`。动画绝不影响功能可用时机。
4. **不打断** — 动画期间输入不被阻塞（除非是路由切换）。

### 5.2 动画参数表

```
用途              属性              时长     缓动
────────────────  ───────────────  ────────  ──────────────────
hover 过渡        background,      120ms    ease
                 color, opacity
active 按压       transform         80ms    ease
focus 切换        outline           80ms    ease
路由切换          opacity           120ms    ease
模态框入场        scale + opacity   150ms    ease-out
模态框退场        scale + opacity   100ms    ease-in
Toast 入场        translateY +      200ms    ease-out
                 opacity
Toast 退场        translateY +      150ms    ease-in
                 opacity
侧边栏折叠        width             200ms    ease
下拉菜单展开      opacity +         150ms    ease-out
                 translateY(-4px)
状态点脉冲        opacity           2000ms   ease-in-out
                 (呼吸循环)                 (infinite)
空闲环动画        stroke-dashoffset 取决于    linear
                  (旋转)           数据
```

### 5.3 微交互

**按钮按下：**
```css
transition: background 0.15s, transform 0.1s;
&:active { transform: scale(0.98); }
```
按压反馈是 Aman 中最重要的微交互——它告诉用户"操作已被接收"。不提供按压反馈的按钮是死的。

**链接/导航悬停：**
```css
transition: background 0.12s, color 0.12s;
```
导航项从 `fg-secondary` 过渡到 `fg`，背景从透明过渡到 `bg-hover`。没有位移，没有下划线动画。

**加载指示器：**
- 不使用旋转器 (spinner)，除非等待时间 > 500ms
- 优先使用骨架屏（skeleton）——形状暗示内容，颜色脉冲暗示加载中
- 流式文本（逐字出现）是 Aman 的特色——这是"思维可见"的设计，不应被加载动画替代

### 5.4 不使用的动画

- ❌ 页面加载时的元素逐个飞入/淡入（"PPT 动画"）
- ❌ 滚动触发的视差或显示动画
- ❌ 悬浮/漂浮的装饰性动画
- ❌ 光标跟随效果
- ❌ 粒子效果或庆祝动画
- ❌ 打字机光标闪烁（使用静态光标或极慢脉冲代替）

---

## 6. 状态与边界

### 6.1 状态覆盖矩阵

Aman 的每个组件都必须覆盖以下状态。没有例外。

| 状态 | 说明 | 要求 |
|------|------|------|
| **默认** | 组件在有数据时的正常展示 | 这是设计的起点 |
| **悬停** | 鼠标指针在元素上方 | 120ms 内响应；触屏设备跳过 |
| **激活/按下** | 元素正在被点击 | scale(0.98)；< 100ms |
| **聚焦** | 键盘导航选中元素 | outline: 2px accent；仅在 :focus-visible 时显示 |
| **禁用** | 元素不可交互 | opacity: 0.4 + cursor: default；保留元素空间 |
| **加载中** | 数据正在获取 | 骨架屏或流式内容；不空白 |
| **空** | 数据为空（不是加载中） | 显示说明文字 + 操作引导；不是空白页 |
| **错误** | 数据获取失败 | 显示错误原因 + 重试操作；不隐藏 |
| **极限** | 数据量极大 | 虚拟滚动或分页；不卡顿 |

### 6.2 空状态设计

空状态是 Aman 的"第一印象"——新用户看到的第一屏通常是空的（没有 Agent、没有 Provider、没有聊天记录）。

**空状态设计规则：**
1. **告知这是什么区域** — 一句话说明这个区域的用途
2. **告知为什么是空的** — "你还没有创建 Agent" > "暂无数据"
3. **提供下一步操作** — 一个 Primary 按钮引导用户完成第一步
4. **不道歉** — 不要说"抱歉，这里什么都没有"。空是正常的初始状态。
5. **不卖萌** — 不插图、不拟人、不说"这里好空旷啊~"

**好的空状态示例：**
```
Agent 列表为空
创建你的第一个 AI Agent，赋予它名字和灵魂。

[+ 创建 Agent]
```

**不好的空状态示例：**
```
❌ "No agents found."（没说为什么、怎么办）
❌ "这里什么都没有~" （卖萌，没有信息量）
❌ 一张大插图 + "暂无数据" （浪费空间，没有行动引导）
```

### 6.3 错误状态设计

Aman 的后端是防御性工程——UI 的错误处理应该同样严谨。

**错误状态设计规则：**
1. **说清楚发生了什么** — "无法连接到 Gateway" > "出错了"
2. **说清楚可能的原因** — "Gateway 进程可能未启动"（如果检测到）
3. **提供操作选项** — 重试、启动 Gateway、查看日志
4. **错误信息使用次要文本色** — 错误已经发生了，不让红色文本加剧用户的焦虑
5. **保留用户已输入的数据** — 表单错误后不清空已填内容

### 6.4 加载状态设计

**加载的三种策略（按优先级排列）：**

1. **骨架屏** — 用于首次加载（页面刚打开，数据从未有过）。骨架形状与真实内容相同，颜色脉冲暗示活动。
2. **流式内容** — 用于 LLM 回复（逐字/逐块出现）。不额外显示加载指示器——内容本身就是加载指示器。
3. **进度条** — 用于已知时长的操作（文件上传、大量数据处理）。显示百分比或步骤数。

**加载状态规则：**
- 操作发起后 200ms 内不显示加载指示器（大多数操作在 200ms 内完成）
- 200-500ms: 显示微小加载状态（按钮内文字变为 "..."）
- 500ms+: 显示骨架屏或进度条
- 10s+: 显示"仍在处理"提示 + 取消按钮
- 30s+: 显示超时提示 + 联系支持引导

### 6.5 破坏性操作确认

Aman 对破坏性操作使用分级确认策略：

```
操作风险      确认方式
──────────── ──────────────────────
低            无确认（撤销成本低：删除消息草稿、切换标签）
中            Toast 确认 + 撤销（删除 Agent、禁用插件）
高            模态框确认 + 输入确认文字（删除所有数据、重置配置）
```

**模态框确认模板：**
```
标题: [操作名称]
正文: [操作后果，用简洁的语言说明不可逆性]
输入确认: "输入 [关键词] 以确认"
按钮: [取消] [红色确认按钮]
```

---

## 7. 文案与语气

### 7.1 Aman 的 UI 声音

Aman 的 SOUL 说："简洁时像终端输出，详细时像技术文档。"

UI 文案继承同样的声音：

| 特质 | 表现 | 反例 |
|------|------|------|
| **简洁** | "Agent 已创建" | "恭喜！你成功创建了一个 Agent！🎉" |
| **准确** | "无法连接到 Gateway 进程" | "好像出了点问题" |
| **直接** | "删除此 Agent 将同时删除其所有会话记录" | "你确定要这样做吗？此操作的影响可能很大" |
| **不讨好** | "没有新通知" | "暂时没有新消息，去喝杯咖啡吧~ ☕" |
| **不傲慢** | "我们遇到一个错误，正在重试" | "发生了一个内部错误 (Error Code: 0x8A3F)" |

### 7.2 按钮文案

```
操作            推荐文案              避免
────────────── ────────────────────  ──────────────────
创建            "创建 Agent"         "新建"、"Add"
删除            "删除 Agent"         "移除"（模糊）
保存            "保存"               "提交"、"确认"
取消            "取消"               "放弃"、"返回"
重试            "重试"               "再试一次"
启动            "启动 Gateway"       "开启"、"运行"
停止            "停止 Gateway"       "关闭"、"暂停"
确认删除        "删除" (红色)         "是的"、"确定"
```

### 7.3 中英双语规则

- UI 标签默认语言跟随系统语言（通过 `navigator.language` 检测）
- 用户可以在设置中切换语言偏好
- 特殊术语保持原文不翻译：Gateway、Agent、SOUL、Pipeline、Workflow、Plugin
- 中文使用全角标点（。、，！？）；英文使用半角标点
- 中英混排时，中文字符与英文单词之间自动加空格（通过 CSS `word-spacing` 或预处理）
- 代码、路径、命令、键名永远不翻译

### 7.4 日期与时间格式

```
场景            格式                      示例
────────────── ────────────────────────  ──────────────────
相对时间(<1h)   "刚刚" / "X 分钟前"       "3 分钟前"
相对时间(<24h)  "X 小时前"               "5 小时前"
相对时间(<7d)   "X 天前"                 "3 天前"
绝对时间        "MM-DD HH:mm"            "06-01 14:30"
完整时间        "YYYY-MM-DD HH:mm:ss"    "2026-06-01 14:30:00"
```

---

## 8. 可访问性

### 8.1 对比度

所有文本组合必须满足 WCAG 2.1 AA 标准：

- 正文文本 (14px, 400w): ≥ 4.5:1 对比度（Aman 暗色主题 `#e4e6f0` 对 `#0b0d13` = ~13:1 ✓）
- 大文本 (≥18px 或 ≥14px 600w): ≥ 3:1 对比度
- 辅助文本: ≥ 3:1 对比度（`#9a9db5` 对 `#0b0d13` = ~7:1 ✓）
- 禁用文本不要求满足对比度标准

### 8.2 键盘导航

- 所有交互元素必须可通过键盘访问（Tab 导航）
- 可见的聚焦指示器（`:focus-visible` outline）
- Tab 顺序 = 视觉顺序（从左到右，从上到下）
- 模态框打开时，焦点锁定在模态框内
- Escape 关闭模态框、下拉菜单、弹出层
- Enter/Space 激活按钮和链接
- 常用操作有快捷键（键盘快捷键在单独文档中定义）

### 8.3 屏幕阅读器

- 所有图标按钮必须有 `aria-label`
- 状态变化使用 `aria-live` 区域通知
- 页面标题使用 `<h1>`，区块标题使用 `<h2>`
- 装饰性 SVG 使用 `aria-hidden="true"`
- 通知横幅使用 `role="alert"`
- 表单错误使用 `aria-describedby` 关联错误信息

### 8.4 减少动效

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

遵守 `prefers-reduced-motion` 媒体查询。当用户设置减少动效时：
- 所有动画持续时间 < 10ms（实质上是即时的）
- 流式文本直接显示完整内容（跳过逐字动画）
- 空闲环停止旋转动画

---

## 9. 页面设计模式

### 9.1 仪表板型页面（Home, Dashboard）

**特点：** 概览、状态指示、快速操作入口

**布局：** CSS Grid 卡片网格（`.grid-2` 或 `.grid-3`），顶部可有统计条

**规则：**
- 最重要的信息放在左上角（F 型扫描模式）
- 统计数字大而醒目（28px / 700w / accent 色）
- 每张卡片有一个明确的主题，不混合不相关信息
- 卡片按功能分组，组间有更大间距（24px）

### 9.2 对话型页面（Chat）

**特点：** 消息列表 + 输入框

**布局：** 消息区 flex-1 可滚动 + 底部固定输入区

**规则：**
- 用户消息右对齐（accent 背景），AI 消息左对齐（bg-card 背景 + 边框）
- 消息气泡圆角 18px（> 卡片圆角，营造对话感）
- 流式文本逐块出现，不模拟打字机效果
- 工具调用以可展开卡片形式内嵌在消息流中
- 新消息从底部出现（自动滚动到底部，但用户上滚查看历史时不强制滚动）

### 9.3 管理型页面（Agents, Providers, Integration）

**特点：** 列表 + 创建/编辑表单

**布局：** 顶部操作栏 + 列表/表格 + 侧边编辑面板或内联编辑

**规则：**
- 列表项悬停显示操作按钮（编辑、删除）
- 创建入口为列表顶部的主按钮
- 删除操作使用二级确认（Toast + 撤销 或 对话框确认，视风险而定）
- 表单验证在失焦时触发（不是提交时才发现错误）

### 9.4 监控型页面（Workflow Board, Maintenance）

**特点：** 实时数据、状态机可视化、日志

**布局：** 时间线/看板/表格

**规则：**
- 实时数据应有时间戳，让用户知道数据的新鲜度
- 使用等宽字体展示日志、JSON、堆栈跟踪
- 状态机可视化使用节点 + 连线图（可考虑用 CSS 而非库实现）
- 数据自动刷新但可暂停（暂停按钮 + "X 秒前更新" 指示器）

### 9.5 编辑器型页面（Soul Editor, Config）

**特点：** 文本编辑 / 结构化编辑

**规则：**
- 使用等宽字体（代码编辑器感）
- 支持语法高亮（Markdown 至少支持标题、代码块、粗体/斜体）
- 编辑后自动保存（debounce 2s）或 Ctrl+S 手动保存
- 显示字符数和行数

---

## 10. 设计令牌速查

### 10.1 CSS 变量（暗色主题，默认）

```css
/* 表面 */
--bg: #0b0d13;
--bg-elevated: #11141c;
--bg-card: #161923;
--bg-hover: #1e2130;
--bg-active: #262a3a;

/* 文本 */
--fg: #e4e6f0;
--fg-secondary: #9a9db5;
--fg-dim: #6b6e80;

/* 强调 */
--accent: #5b7df5;
--accent-hover: #7b98ff;
--accent-muted: rgba(91, 125, 245, 0.12);

/* 语义 */
--green: #3dbf6e;
--green-muted: rgba(61, 191, 110, 0.12);
--yellow: #e5b80b;
--yellow-muted: rgba(229, 184, 11, 0.12);
--red: #e55555;
--red-muted: rgba(229, 85, 85, 0.12);

/* 边框 */
--border: #1e2130;
--border-strong: #2e3142;

/* 圆角 */
--radius-sm: 4px;
--radius-md: 6px;
--radius-lg: 8px;
--radius-xl: 12px;

/* 阴影 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.5);
--shadow-xl: 0 12px 40px rgba(0, 0, 0, 0.6);

/* 排版 */
--font-ui: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
           Helvetica, Arial, sans-serif;
--font-mono: "SF Mono", "Fira Code", "Cascadia Code",
             "JetBrains Mono", "Consolas", monospace;
--font-size-base: 14px;
--line-height-base: 1.5;
```

### 10.2 间距刻度

```
xs:  4px
sm:  8px
md:  12px
lg:  16px
xl:  20px
2xl: 24px
3xl: 28px
```

### 10.3 字体尺寸

```
微小:  10px / 500-600w / 1.3lh
标签:  11px / 500-600w / 1.3lh
辅助:  13px / 400-450w / 1.5lh
正文:  14px / 400w    / 1.5lh
标题:  14px / 600w    / 1.4lh  (卡片标题)
区块:  16px / 600w    / 1.4lh
页面:  20px / 700w    / 1.3lh
数值:  28px / 700w    / 1.2lh  (统计数字)
```

---

## 附录 A: 设计决策记录

当 UI 设计面临"两个都合理"的选择时，按以下优先级裁定：

1. **信息密度 > 视觉呼吸** — 工具型应用，不是营销页面
2. **清晰 > 优雅** — 用户需要理解，不需要被惊艳
3. **快速 > 流畅** — 100ms 的快 > 400ms 的流畅动画
4. **安静 > 活跃** — 界面静止时是"在休息"，不是"死掉了"
5. **直接 > 礼貌** — "创建失败：名称已存在" > "抱歉，我们无法完成您的请求"

## 附录 B: 常见反模式

| 反模式 | 说明 | 正确做法 |
|--------|------|---------|
| 空洞的仪表板 | 用大数字填满空间但没有可操作信息 | 每个指标要么可点击深入，要么旁边有趋势/对比 |
| 过度通知 | 每个小操作都弹 toast | 只有失败和破坏性操作需要 toast；成功是默认的 |
| 隐藏的错误 | 把错误信息藏在"详情"折叠里 | 错误原因直接展示，折叠的是技术细节 |
| 不一致的删除 | 有些地方弹对话框，有些地方直接删 | 按破坏性分级确认（见 §6.5） |
| 空白恐惧 | 用插图、引导视频、表情符号填满空状态 | 空状态 = 一句话 + 一个按钮（见 §6.2） |
| 动画竞赛 | 每个页面加载都有一串飞入动画 | 无入场动画是默认的；动画只在传达变化时使用 |

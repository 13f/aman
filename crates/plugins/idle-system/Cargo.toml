[package]
name = "idle-system"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Idle personality system — handles all 7 IdleKind states (daze, boredom, sleep, exploration, meditation, waiting, incubation)"
publish = false

[lints]
workspace = true

[dependencies]
kernel = { path = "../../core" }
serde_json = "1"
async-trait = "0.1"
semver = "1"
tracing = "0.1"
tokio = { version = "1", features = ["sync", "rt", "macros"] }

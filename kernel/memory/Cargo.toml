[package]
name = "memory"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Aman memory providers — pluggable backends for agent long-term memory"

[lints]
workspace = true

[dependencies]
kernel = { path = "../core", version = "0.1.0" }
cognitive-llm = { path = "../../cognitive/llm" }
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
yantrikdb = { version = "0.7", default-features = false, features = ["embedder-download"] }
tokio = { version = "1", features = ["sync", "rt", "macros", "rt-multi-thread"] }

[dev-dependencies]
tempfile = "3"

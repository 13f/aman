# Implementation Plans

Generated by the improve skill on 2026-06-16 (commit `ec2cddf`).
Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix SecretCache Default producing all-zero encryption key | P0 | S | — | DONE |
| 002 | Fix JSON-RPC empty-string response on serialization failure | P1 | S | — | DONE |
| 003 | Redact sensitive data in startup eprintln! messages | P1 | S | — | DONE |
| 004 | Fix Relaxed memory ordering on plugin bridge shutdown flag | P1 | S | — | DONE |
| 005 | Fix CLI --help flag and binary naming consistency | P1 | S | — | DONE |
| 006 | Remove stale --daemon / --log-level flags from CLI.md | P2 | S | — | DONE |
| 007 | Fix stale crates/ paths in docs | P2 | S | — | DONE |
| 008 | Implement TraceStore for SQLite-backed trace persistence | P1 | M | — | DONE |
| 009 | Implement MemoryProvider store and recall | P1 | M | — | DONE |
| 010 | Wire ToolSecurityConfig into agent harness | P1 | L | — | DONE |
| 011 | Replace blocking std::sync::Mutex in Pipeline ConcurrencyController with tokio primitives | P1 | L | — | TODO |
| 012 | Log silent event bus publish errors at ~30 call sites | P2 | L | — | TODO |
| 013 | Add characterization tests for gateway core paths | P2 | L | — | TODO |
| 014 | Add characterization tests for lifestyle crates | P3 | L | 008, 009 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 008 and 009 (TraceStore + MemoryProvider impls) are prerequisites for 014 (lifestyle crate tests), since lifestyle subsystems depend on trace and memory backends.
- 010 (ToolSecurityConfig) is independent but high-risk; recommend characterization tests (013) land first.
- 011 (Pipeline async fix) touches core concurrency infrastructure; recommend 013 (gateway tests) land first as a safety net.
- 001–007 are independent S-effort fixes that can be done in any order or in parallel.
- 012 (silent event bus errors) touches ~30 sites across many crates; coordinate with 013 to avoid merge conflicts.

## Findings considered and rejected

- Plugin bridge reader `Err(_) => break`: By-design — the comment says "pipe closed"; `BufReader::lines()` returns `Err` on EOF/broken pipe, not transient errors.
- `std::thread::sleep` in `kill_process()` at `agent_harness.rs:193`: Function is synchronous (`fn kill_process(pid: u32)`), called during process cleanup/shutdown, not on the hot async path. Negligible impact.
- `resolve_env` silent empty substitution: `unwrap_or_default()` for missing env vars in info-hub config templates is a reasonable default for optional config. By-design.
- `serde_yaml` version drift (0.9 vs 0.9.34): Minor-version pinning inconsistency is cosmetic; not worth a dedicated plan at current scale.

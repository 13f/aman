[package]
name = "hello-skill"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Aman SDK example: hello-skill plugin demonstrating Skill, Tool, and Plugin traits"
publish = false

[lints]
workspace = true

[dependencies]
sdk = { path = "../.." }
serde_json = "1"
async-trait = "0.1"
semver = "1"
tokio = { version = "1", features = ["sync"] }

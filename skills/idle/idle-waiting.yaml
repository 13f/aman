name: idle-waiting
version: "1.0.0"
description: >
  Intermediate idle state with Passive arousal decay — normal attenuation.
  The agent is waiting for external input or pending operations to complete.
  Unlike Daze, Waiting implies an expectation of future activity.
triggers:
  - event_types: ["idle"]
    sources: ["idle.system"]
concurrency: serial
